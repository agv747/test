{
  "name": "retail-price-intelligence-sg",
  "version": "2.0.0",
  "private": true,
  "type": "module",
  "description": "Retail Price Intelligence — Singapore. Mobile-first field price capture + manager decision-support.",
  "scripts": {
    "dev": "wrangler dev",
    "start": "wrangler dev",
    "deploy": "wrangler deploy",
    "test": "node --test \"tests/*.test.js\"",
    "serve": "node scripts/static-server.js",
    "smoke": "node scripts/smoke.mjs",
    "demo-images": "node scripts/make-demo-images.mjs"
  },
  "devDependencies": {
    "playwright": "^1.63.0",
    "wrangler": "^4.0.0"
  }
}
